{
  "name": "Universal + R via apt",
  "image": "bioconductor/bioconductor_docker:RELEASE_3_17",
  "customizations": {
    "vscode": {
      "extensions": [
        "ms-python.python",
        "ms-toolsai.jupyter",
        "REditorSupport.r",
        "Iterative.dvc"
      ]
    }
  },
  "postCreateCommand": "sudo apt-get update && sudo apt-get install -y r-base r-base-dev libcurl4-openssl-dev libxml2-dev libssl-dev && pip install -r requirements.txt"
}
